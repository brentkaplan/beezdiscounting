% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb-methods.R
\name{augment.beezdiscounting_tmb}
\alias{augment.beezdiscounting_tmb}
\title{Augment a beezdiscounting_tmb model}
\usage{
\method{augment}{beezdiscounting_tmb}(x, newdata = NULL, ...)
}
\arguments{
\item{x}{A \code{beezdiscounting_tmb} object.}

\item{newdata}{Optional data frame. \code{NULL} uses the fitting data.}

\item{...}{Unused.}
}
\value{
A tibble with the same rows as the data plus \code{.fitted}, \code{.resid},
and \code{.std_resid}.
}
\description{
Returns the data used for fitting (or \code{newdata}) as a tibble with three
diagnostic columns appended:
\itemize{
\item \code{.fitted}: subject-conditional fitted indifference proportion (clamped to
\verb{(0, 1)}).
\item \code{.resid}: raw residual \code{y - .fitted} on the response scale.
\item \code{.std_resid}: Pearson (standardized) residual - \code{.resid} divided by the
per-row response SD.  For \code{family = "gaussian"} the SD is the constant
\code{sigma_e}; for \code{family = "sltb"} it is the delta-method SLT SD
\code{sqrt(mu * (1 - mu) / (phi + 1))}.
}
}
\examples{
\donttest{
dd <- simulate_dd_ip(n_subjects = 20, seed = 1)
fit <- fit_dd_tmb(dd, verbose = 0)
head(augment(fit))
}

}
