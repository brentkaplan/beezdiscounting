% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-linear-methods.R
\name{beezdiscounting_linear-methods}
\alias{beezdiscounting_linear-methods}
\alias{print.beezdiscounting_linear}
\alias{summary.beezdiscounting_linear}
\alias{print.summary.beezdiscounting_linear}
\alias{tidy.beezdiscounting_linear}
\alias{glance.beezdiscounting_linear}
\alias{coef.beezdiscounting_linear}
\alias{confint.beezdiscounting_linear}
\alias{augment.beezdiscounting_linear}
\alias{predict.beezdiscounting_linear}
\alias{nobs.beezdiscounting_linear}
\alias{logLik.beezdiscounting_linear}
\title{Methods for linearized Mazur fits}
\usage{
\method{print}{beezdiscounting_linear}(x, ...)

\method{summary}{beezdiscounting_linear}(object, ...)

\method{print}{summary.beezdiscounting_linear}(x, ...)

\method{tidy}{beezdiscounting_linear}(x, effects = c("subject", "population"), ...)

\method{glance}{beezdiscounting_linear}(x, ...)

\method{coef}{beezdiscounting_linear}(object, ...)

\method{confint}{beezdiscounting_linear}(object, parm = c("population", "subject"), level = NULL, ...)

\method{augment}{beezdiscounting_linear}(x, ...)

\method{predict}{beezdiscounting_linear}(object, type = "parameters", ...)

\method{nobs}{beezdiscounting_linear}(object, ...)

\method{logLik}{beezdiscounting_linear}(
  object,
  level = c("population", "subject"),
  scale = c("raw", "transformed"),
  ...
)
}
\arguments{
\item{...}{Unused; present for S3 generic consistency.}

\item{object, x}{A \code{beezdiscounting_linear} fit (for \code{print.summary()}, the
object returned by \code{summary()}).}

\item{effects}{For \code{tidy()}: \code{"subject"} (default) returns the per-unit
table; \code{"population"} returns the random-effects MLEs \verb{mu_<level>},
\code{sigma2} and \code{g}.}

\item{parm}{Which interval \code{confint()} returns: \code{"population"} (default) is
an ANOVA-style t-interval on each condition mean, using the between-unit
mean square with \code{N - C} degrees of freedom; \code{"subject"} gives the
per-subject t-intervals on ln k. It does not select parameter names.}

\item{level}{For \code{confint()}, the confidence level. \code{NULL} (default) means
\code{0.95} for \code{parm = "population"}, as in the package's other \code{confint()}
methods, and the fit's \code{conf_level} for \code{parm = "subject"}, so that call
reproduces the intervals in \code{tidy()} / \code{fit$subjects}.
For \code{logLik()}, which likelihood to return: \code{"population"} (the
random-effects MLE, \code{df} = C + 2) or \code{"subject"} (the sum of the per-unit
log-likelihoods, \code{df} = 2 per unit).}

\item{type}{For \code{predict()}: only \code{"parameters"} exists, returning the
per-subject \verb{ln k} and \code{k} with their t-intervals (\code{id}, \code{condition},
\code{logk}, \code{logk_lower}, \code{logk_upper}, \code{k}, \code{k_lower}, \code{k_upper}). There is
no \code{newdata} prediction; \code{augment()} gives the fitted curve.}

\item{scale}{For \code{logLik()}: \code{"raw"} (default) is Jacobian-corrected back to
the indifference-point scale; \code{"transformed"} is on the linearized scale.}
}
\value{
\code{print()} and \code{print.summary()} return their input invisibly;
\code{summary()} returns an object of class \code{summary.beezdiscounting_linear};
\code{tidy()}, \code{glance()}, \code{augment()} and \code{predict()} return tibbles.
\code{augment()} adds \code{.fitted} and \code{.resid} on the raw indifference-point
scale and \code{.std_resid}, the transformed-scale residual \verb{y_lin - ln k_i}
divided by the model's error standard deviation (\code{sqrt(sigma2)} from the
random-effects fit, or the pooled within-subject mean square when that
component is unavailable); \code{.std_resid} is \code{NA} for points dropped under
\code{boundary = "drop"}. \code{coef()} returns the
named vector of condition means \code{mu}; \code{confint()} returns a two-column
matrix of lower/upper bounds; \code{nobs()} returns the number of usable
transformed observations; \code{logLik()} returns a \code{"logLik"} object with \code{df}
and \code{nobs} attributes. Methods needing the random-effects component error
when the design was unbalanced and \code{re} is \code{NULL}.
}
\description{
S3 methods for objects of class \code{beezdiscounting_linear} returned by
\code{\link[=fit_dd_linear]{fit_dd_linear()}}.
All quantities are closed form; nothing is re-optimized.
}
\details{
\code{confint()} uses \code{parm} to SELECT WHICH INTERVAL is returned, not to filter
parameter names as in \code{\link[stats:confint]{stats::confint()}}.
\code{logLik()} reports the raw-scale (Jacobian-corrected) log-likelihood by
default, which makes it comparable
with this package's Gaussian NLS and TMB log-likelihoods.
}
\seealso{
\code{\link[=fit_dd_linear]{fit_dd_linear()}},
\code{\link[=anova.beezdiscounting_linear]{anova.beezdiscounting_linear()}},
\code{\link[=plot.beezdiscounting_linear]{plot.beezdiscounting_linear()}}
}
