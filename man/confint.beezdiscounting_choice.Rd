% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-choice-methods.R
\name{confint.beezdiscounting_choice}
\alias{confint.beezdiscounting_choice}
\title{Confidence intervals for a structural choice discounting model}
\usage{
\method{confint}{beezdiscounting_choice}(
  object,
  parm = NULL,
  level = 0.95,
  report_space = c("internal", "natural"),
  ...
)
}
\arguments{
\item{object}{A \code{beezdiscounting_choice} object.}

\item{parm}{Optional character vector for filtering. Accepts display names
(\code{"k:(Intercept)"}, \code{"gamma"}) \strong{or} raw optimizer names (\code{"beta_k"},
\code{"log_gamma"}, \code{"log_sigma_u"}, \code{"beta0"}). \code{NULL} returns all
coefficients.}

\item{level}{Confidence level (default \code{0.95}).}

\item{report_space}{\code{"internal"} (default; all coefficients on their
estimation scale) or \code{"natural"} (exponentiate the \code{beta_k} rows so the
intercept is \code{k} at the reference level, and exponentiate \code{log_gamma} so
the row is \code{gamma}; \code{log_sigma_u} and \code{beta0} stay on their internal
scales).}

\item{...}{Unused.}
}
\value{
A tibble with columns \code{term}, \code{estimate}, \code{conf.low}, \code{conf.high},
\code{level}.
}
\description{
Wald (Hessian-based) confidence intervals: \verb{estimate +/- z * se} on the
internal scale, then back-transformed to \code{report_space}.
}
