% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb-methods.R
\name{confint.beezdiscounting_tmb}
\alias{confint.beezdiscounting_tmb}
\title{Confidence intervals for a TMB discounting model}
\usage{
\method{confint}{beezdiscounting_tmb}(
  object,
  parm = NULL,
  level = 0.95,
  report_space = c("internal", "natural"),
  ...
)
}
\arguments{
\item{object}{A \code{beezdiscounting_tmb} object.}

\item{parm}{Optional character vector for filtering. Accepts display names
(\code{"k:(Intercept)"}, and \code{"s"} for the GM/Rachlin shape parameter) \strong{or} raw
optimizer names (\code{"beta_k"}, \code{"log_sigma_u"}, \code{"log_phi"}, \code{"log_sigma_e"},
\code{"log_s"}, and for a 2-RE fit \code{"log_sd_re"} / \code{"cor_re"}). \code{NULL} returns
all coefficients.}

\item{level}{Confidence level (default \code{0.95}).}

\item{report_space}{\code{"internal"} (default; all coefficients on their
estimation/log scale) or \code{"natural"} (exponentiate \code{beta_k} rows so the
intercept is \code{k} at the reference level and non-intercept terms are
multiplicative fold-changes; variance/aux params stay on their internal
scale).}

\item{...}{Unused.}
}
\value{
A tibble with columns \code{term}, \code{estimate}, \code{conf.low}, \code{conf.high},
\code{level}.
}
\description{
Wald (Hessian-based) confidence intervals: \verb{estimate +/- z * se} on the
internal (log-k) scale, then back-transformed to \code{report_space}.
}
\note{
For a 2-RE fit the \code{cor_re} and \code{log_sd_re} rows are reported on their
internal (atanh / log) scales and are NOT back-transformed by
\code{report_space = "natural"} (only \code{beta_k}/\code{log_s} rows are); use
\code{VarCorr()} to obtain the correlation and natural-log SDs.
}
\examples{
\donttest{
dd <- simulate_dd_ip(n_subjects = 20, seed = 1)
fit <- fit_dd_tmb(dd, equation = "mazur", family = "sltb",
                  random_effects = k ~ 1, verbose = 0)
confint(fit)
confint(fit, parm = "k:(Intercept)", report_space = "natural")
}

}
