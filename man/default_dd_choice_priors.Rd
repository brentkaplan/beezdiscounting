% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-brms-priors.R
\name{default_dd_choice_priors}
\alias{default_dd_choice_priors}
\title{Default priors for the Bayesian (brms) choice model}
\usage{
default_dd_choice_priors(
  equation = c("mazur", "exponential"),
  intercept = FALSE,
  data = NULL,
  delay_var = "delay",
  autoscale = !is.null(data)
)
}
\arguments{
\item{equation}{\code{"mazur"} or \code{"exponential"}.}

\item{intercept}{Include the \code{b0} prior.}

\item{data}{Optional data frame used for \code{logk} autoscaling.}

\item{delay_var}{Delay column name in \code{data}.}

\item{autoscale}{Logical; defaults to \code{TRUE} when \code{data} is supplied.}
}
\value{
A \code{brmsprior} data frame.
}
\description{
\code{loggamma ~ normal(1, 1)} (choice sensitivity, typically 1-20) and the
optional logit-scale bias \code{b0 ~ normal(0, 1.5)}; \code{logk} defaults as in
\code{\link[=default_dd_priors]{default_dd_priors()}} (autoscaled to the median delay when \code{data} is
supplied; \code{delay_var} names the delay column).
}
