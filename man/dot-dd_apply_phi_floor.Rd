% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{.dd_apply_phi_floor}
\alias{.dd_apply_phi_floor}
\title{Impose the SLT-beta phi floor as a log_aux lower bound}
\usage{
.dd_apply_phi_floor(tmb_control, family)
}
\arguments{
\item{tmb_control}{Merged control list (may carry user \code{lower}/\code{upper}).}

\item{family}{One of "sltb", "gaussian".}
}
\value{
The (possibly augmented) \code{tmb_control} list.
}
\description{
For \code{family == "sltb"} the degenerate phi->0 / k->inf optimum (spec section
4.8) is blocked by a \strong{lower bound on \code{log_aux}} of \code{log(.dd_phi_min)}
inserted into \code{tmb_control$lower}. This must apply to BOTH optimizer paths
(single fit and multi-start), so the logic lives in one shared helper. The
bound is overridable: if the caller already supplies a \code{log_aux} entry in
\code{tmb_control$lower}, their value wins (the floor is not added).
}
\keyword{internal}
