% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{.dd_apply_s_bounds}
\alias{.dd_apply_s_bounds}
\title{Impose wide optimizer bounds on log_s for the 2-parameter equations}
\usage{
.dd_apply_s_bounds(tmb_control, has_s)
}
\arguments{
\item{tmb_control}{Merged control list (may carry user \code{lower}/\code{upper}).}

\item{has_s}{Logical; \code{TRUE} for green-myerson / rachlin.}
}
\value{
The (possibly augmented) \code{tmb_control} list.
}
\description{
Mirrors \code{\link[=.dd_apply_phi_floor]{.dd_apply_phi_floor()}}: adds \code{log_s} lower/upper entries to
\code{tmb_control$lower}/\verb{$upper} (wide numerical guards \verb{[log(0.05), log(20)]})
only when \code{has_s} and only when the caller has not already set a \code{log_s}
bound (user value wins). For 1-parameter equations \code{log_s} is mapped out of
the optimizer vector, so no bound is added (and \code{.expand_bounds} would
otherwise warn about an unknown parameter).
}
\keyword{internal}
