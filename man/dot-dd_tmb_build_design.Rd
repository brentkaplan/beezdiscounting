% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{.dd_tmb_build_design}
\alias{.dd_tmb_build_design}
\title{Build the fixed-effect design matrix for log k}
\usage{
.dd_tmb_build_design(
  data,
  factors = NULL,
  factor_interaction = FALSE,
  continuous_covariates = NULL
)
}
\arguments{
\item{data}{Cleaned long data frame (canonical \code{id}/\code{x}/\code{y} plus any factor
or covariate columns).}

\item{factors}{Character vector of factor column names, or \code{NULL}.}

\item{factor_interaction}{Logical; if \code{TRUE} and >= 2 factors, include their
interaction (uses \code{*}); otherwise main effects (\code{+}).}

\item{continuous_covariates}{Character vector of covariate names, or \code{NULL}.}
}
\value{
A list with \code{X} (the model matrix for log-k FE), \code{rhs} (the one-sided
RHS \strong{formula} from \code{\link[beezdemand:build_fixed_rhs]{beezdemand::build_fixed_rhs()}}), and \code{contrasts}
(\code{attr(X, "contrasts")}, the per-factor contrast scheme). The \code{rhs} and
\code{contrasts} are stored so \code{.dd_build_emm_ref_grid()} can rebuild a
column-aligned design via the same route.
}
\description{
Constructs the \code{model.matrix} for the \verb{log k = Xbeta} linear predictor from
between-subject factors, an optional pairwise interaction, and continuous
covariates. With no inputs the design is intercept-only.
}
\keyword{internal}
