% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{.dd_tmb_compute_subject_pars}
\alias{.dd_tmb_compute_subject_pars}
\title{Compute subject-specific discounting parameters}
\usage{
.dd_tmb_compute_subject_pars(
  coefficients,
  u_hat,
  subject_levels,
  design_X,
  subject_id,
  equation,
  family,
  n_re = 1L,
  log_aux_name = "log_phi",
  Sigma = NULL,
  re2_target = 0L
)
}
\arguments{
\item{coefficients}{Named coefficient vector (with \code{beta_k}, \code{log_sigma_u}
for 1-RE, and \code{log_phi} or \code{log_sigma_e}).}

\item{u_hat}{Matrix of per-subject random effects: \verb{n_subjects x 1}
standardized \code{u} deviates (1-RE) or \verb{n_subjects x 2} standardized \code{b}
deviates (2-RE).}

\item{subject_levels}{Character vector of subject ids (length n_subjects).}

\item{design_X}{Fixed-effect design matrix (rows aligned with the prepared
arrays); columns correspond 1:1 with the \code{beta_k} entries.}

\item{subject_id}{Integer 0-indexed subject map (length \code{n_obs}), aligned
row-for-row with \code{design_X}. Used to locate each subject's first design row.}

\item{equation}{One of "mazur", "exponential" (reserved; k is equation-free).}

\item{family}{One of "sltb", "gaussian".}

\item{n_re}{Number of random-effect intercepts (\code{1L} or \code{2L}). Defaults to
\code{1L} so existing positional callers stay valid.}

\item{log_aux_name}{Name of the auxiliary (precision) coefficient in
\code{coefficients} (\code{"log_phi"} for sltb). Used only for \code{n_re == 2}.}

\item{Sigma}{Fitted 2x2 RE covariance (from \code{\link[=.dd_tmb_extract_estimates]{.dd_tmb_extract_estimates()}});
required for \code{n_re == 2}.}

\item{re2_target}{Integer flag: \code{0L} for a phi-target 2-RE fit, \code{1L} for an
s-target fit. Ignored when \code{n_re == 1L}. Defaults to \code{0L}.}
}
\value{
data.frame: \verb{id, u_i, k} (1-RE), \verb{id, re_k, re_phi, k, phi}
(phi-target 2-RE), or \verb{id, re_k, re_s, k, s} (s-target 2-RE).
}
\description{
Reconstructs each subject's \code{k_i = exp(eta_i)} where
\code{eta_i = X_i \%*\% beta_k + sigma_u * u_i}, exactly matching the C++ template's
non-centered predictor (\code{log_k_i = X.row(i)*beta_k + sigma_u * u(subj,0)}).
\code{X_i} is subject \code{i}'s fixed-effect design ROW. For between-subject
factors/covariates the design is constant within a subject, so the first
design-matrix row for each subject is used. This makes per-subject \code{k}
factor/covariate-correct: subjects in non-reference groups pick up their
group's \code{beta_k} contribution rather than the reference-group intercept.
}
\details{
For a 1-RE fit the auxiliary scalar \code{phi} is population-level, so it is not a
subject-level parameter (the returned frame is \verb{id, u_i, k}). For a phi-target
2-RE fit (\code{k + phi ~ 1}) each subject's natural-scale \code{re = L * b_hat} is
reconstructed (\code{L = chol(Sigma)} lower-triangular; the \code{sdreport} "random"
block holds the STANDARDIZED \code{b_hat}, not \code{re}), giving \verb{id, re_k, re_phi, k, phi} with a per-subject \code{phi_i} floor matching the kernel clamp. For an
s-target 2-RE fit (\code{k + s ~ 1}) the same reconstruction gives \verb{id, re_k, re_s, k, s} with \code{s_i = .dd_soft_clamp_s_log(log_s + re_s)} (approximately
\code{exp(log_s + re_s)} in the interior, smoothly saturating toward \verb{(0.05, 20)}).
}
\note{
Subject-level \code{k} assumes between-subject predictors: the first design
row per subject defines that subject's fixed-effect contribution. A
within-subject-varying covariate would make a single per-subject \code{k}
ill-defined; in that case the first observed row is used.
}
\keyword{internal}
