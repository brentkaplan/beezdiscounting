% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{.dd_tmb_default_starts}
\alias{.dd_tmb_default_starts}
\title{Generate default starting values for the TMB discounting model}
\usage{
.dd_tmb_default_starts(prepared, design, family, equation = "mazur")
}
\arguments{
\item{prepared}{Output from \code{\link[=.dd_tmb_prepare_data]{.dd_tmb_prepare_data()}}.}

\item{design}{Output from \code{\link[=.dd_tmb_build_design]{.dd_tmb_build_design()}}.}

\item{family}{One of "sltb", "gaussian".}

\item{equation}{One of "mazur", "exponential" (for the start inversion).}
}
\value{
A parameters list: \code{beta_k} (length \code{ncol(X)}), \code{log_sigma_u},
\code{log_aux}, \code{log_s} (always present; held fixed for 1-parameter equations),
\code{u} (matrix \code{n_subjects} x 1), plus the joint 2-RE blocks \code{log_sd_re}
(length 2), \code{cor_re} (length 1), and \code{b} (matrix \code{n_subjects} x 2). The
shared C++ template declares every block; the map fixes the inactive ones
per \code{n_re}.
}
\description{
\code{beta_k} intercept is data-driven: invert the discounting function at the
median \code{y} observed at the minimum delay. \code{log_sigma_u = log(0.5)}. The
generic auxiliary scalar \code{log_aux} starts at \code{log(8)} for SLT-beta
(population precision phi) and \code{log(0.1)} for Gaussian (residual SD).
}
\keyword{internal}
