% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb-methods.R
\name{.dd_tmb_guard_info}
\alias{.dd_tmb_guard_info}
\title{Guard, clamp and floor activity at the fitted values}
\usage{
.dd_tmb_guard_info(object)
}
\arguments{
\item{object}{A \code{beezdiscounting_tmb} fit (with \code{subject_pars}).}
}
\value{
list(n_rows, mu_guard_lower, mu_guard_upper, and either
n_s_clamped_lower / n_s_clamped_upper or n_phi_floor when applicable).
}
\description{
Counts, at the subject-level fitted values, the rows with a positive delay
whose raw mean lies outside the \verb{[1e-6, 1 - 1e-6]} guard (\code{x = 0} rows have
mean 1 by definition and are excluded); for \code{k + s ~ 1}, the subjects whose
soft-clamped \code{s} differs materially from the latent value
(\code{\link[=.dd_s_clamp_active]{.dd_s_clamp_active()}}); for \code{k + phi ~ 1}, the subjects whose latent
\code{phi} is below the 0.1 floor. Reporting only (audit F-BZ4-2, F-BZ4-5).
}
\keyword{internal}
