% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{.dd_tmb_multi_start}
\alias{.dd_tmb_multi_start}
\title{Multi-start optimization for the TMB discounting model}
\usage{
.dd_tmb_multi_start(
  tmb_data,
  start_values,
  tmb_control,
  user_specified,
  verbose
)
}
\arguments{
\item{tmb_data}{TMB data list from \code{\link[=.dd_tmb_build_tmb_data]{.dd_tmb_build_tmb_data()}}.}

\item{start_values}{Default starting list from \code{\link[=.dd_tmb_default_starts]{.dd_tmb_default_starts()}}.}

\item{tmb_control}{Merged control list (may carry user \code{lower}/\code{upper}).}

\item{user_specified}{Character vector of user-set \code{tmb_control} fields.}

\item{verbose}{Integer verbosity level.}
}
\value{
A list with elements \code{obj}, \code{opt}, \code{nll}, \code{start_idx},
\code{opt_warnings}.
}
\description{
Builds 3 starting sets (data-driven, low-k, high-k) and runs each through
\code{\link[=.dd_tmb_run_optimizer]{.dd_tmb_run_optimizer()}}. Keeps the lowest \strong{finite} nll among candidates
passing a NaN/blowup sanity check on \code{beta_k[1]}.
}
\details{
\strong{Degenerate-optimum guard (phi->0 / k->inf, spec section 4.8):}
For \code{family == "sltb"} (i.e. \code{tmb_data$family == 0L}) a \strong{lower bound on
\code{log_aux}} of \code{log(.dd_phi_min)} (= \code{log(0.1)}) is inserted into
\code{tmb_control$lower} before every optimizer call so the optimizer can never
walk into the degenerate phi->0 optimum. The bound is overridable: if the
caller already provides a \code{log_aux} entry in \code{tmb_control$lower} that value
wins (merged before the floor check). Genuine low-precision fits (true phi
above the floor, e.g. phi = 2) are retained — the bound only blocks the
pathological phi->0 sink. There is \strong{no post-hoc phi-based rejection}.

If every candidate trips the blowup guard the lowest-nll fit overall is
returned with a warning. If every candidate fails entirely (error or
non-finite nll) the function stops.
}
\keyword{internal}
