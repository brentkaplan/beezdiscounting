% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb-methods.R
\name{.dd_tmb_predict_k}
\alias{.dd_tmb_predict_k}
\title{Rebuild the per-row log-k linear predictor from newdata}
\usage{
.dd_tmb_predict_k(object, newdata, level = c("subject", "population"))
}
\arguments{
\item{object}{A \code{beezdiscounting_tmb} fit.}

\item{newdata}{Data frame with the x var, factor/covariate cols, and (for
\code{level = "subject"}) the id var.}

\item{level}{\code{"subject"} or \code{"population"}.}
}
\value{
Numeric vector of per-row \code{k}, length \code{nrow(newdata)}.
}
\description{
Reconstructs the fixed-effect design from \code{newdata} using the stored RHS
formula and contrasts, multiplies by \code{beta_k}, and (for
\code{level = "subject"}) adds each subject's \code{sigma_u * u_i} deviate looked up
by id. Returns per-row \code{k} on the natural scale.
}
\details{
Unseen factor levels in \code{newdata} are rejected with a clear error rather
than silently zero-padded (R2 fix).
}
\keyword{internal}
