% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{.dd_tmb_prepare_data}
\alias{.dd_tmb_prepare_data}
\title{Prepare data for the TMB mixed-effects discounting model}
\usage{
.dd_tmb_prepare_data(
  data,
  y_var = "y",
  x_var = "x",
  id_var = "id",
  extra_cols = NULL
)
}
\arguments{
\item{data}{Data frame already coerced/clamped by \code{.dd_validate_ip()},
retaining factor/covariate columns alongside id/x/y.}

\item{y_var, x_var, id_var}{Character column names.}

\item{extra_cols}{Character vector of factor/covariate column names that must
also be free of NA for a row to be kept (the union of \code{factors} and
\code{continuous_covariates}). Defaults to none.}
}
\value{
A list with \code{y}, \code{x}, \code{subject_id} (0-indexed integer),
\code{subject_levels}, \code{n_subjects}, \code{n_obs}, and \code{data} (the SINGLE complete-
cased model frame: canonical \code{id}, \code{x}, \code{y} first, then the retained
factor/covariate columns, in \code{data}'s row order).
}
\description{
Operates on the FULL model frame from \code{.dd_validate_ip()} (canonical
\code{id}/\code{x}/\code{y} PLUS any retained factor/covariate columns). It complete-cases
that frame ONCE over all modeling columns (\code{id}, \code{x}, \code{y}, and \code{extra_cols}),
then derives the parallel arrays \code{y}/\code{x}/\code{subject_id} from the same filtered
rows so the later design matrix, TMB arrays, and \code{fit$data} all share one
row order (row-coherence; mirrors beezdemand's \code{data_for_design}). Builds a
0-indexed \code{subject_id} aligned to \code{subject_levels} (the C++ template indexes
\code{u(subject_id, 0)} from 0).
}
\keyword{internal}
