% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{.dd_tmb_run_optimizer}
\alias{.dd_tmb_run_optimizer}
\title{Run a single TMB optimization (nlminb or L-BFGS-B), family-agnostic}
\usage{
.dd_tmb_run_optimizer(obj, start, tmb_control, user_specified, verbose)
}
\arguments{
\item{obj}{TMB objective function object (from \code{TMB::MakeADFun}), with
\verb{$fn}, \verb{$gr}, and \verb{$par}.}

\item{start}{Named numeric vector of starting parameter values.}

\item{tmb_control}{Merged control list; must have fields \code{optimizer},
\code{iter_max}, \code{eval_max}, \code{rel_tol}, \code{lower}, \code{upper}, \code{trace}.}

\item{user_specified}{Character vector of fields the user explicitly set in
\code{tmb_control} (governs \code{trace} precedence).}

\item{verbose}{Integer verbosity level (0 = silent, 1 = progress,
2 = full trace).}
}
\value{
A list with \code{opt} (list of \code{par}, \code{objective}, \code{convergence},
\code{message}) and \code{warnings} (character vector).
}
\description{
Ported verbatim from beezdemand \code{.tmb_run_optimizer} (renamed only).
Dispatches to \code{nlminb} (default) or \code{optim(method = "L-BFGS-B")} and
normalizes the return value so downstream code always sees identical field
names regardless of optimizer: \verb{$par}, \verb{$objective}, \verb{$convergence},
\verb{$message} (guaranteed non-\code{NULL} character).
}
\details{
Optimizer warnings are captured via \code{withCallingHandlers} and returned in
\verb{$warnings}; errors yield \verb{convergence = 99L, objective = Inf}.
}
\keyword{internal}
