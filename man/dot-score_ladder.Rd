% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instrument-registry.R
\name{.score_ladder}
\alias{.score_ladder}
\title{Score one ascending ladder by consistency maximization}
\usage{
.score_ladder(resp, vals, edge)
}
\arguments{
\item{resp}{0/1/NA responses in ladder order (1 = chose the larger
delayed/risky option). Any \code{NA} makes all three outputs \code{NA} --
impute upstream.}

\item{vals}{Ascending indifference values (k or h), one per item; must be
the same length as \code{resp}.}

\item{edge}{Value appended past the steepest item (the overall-ladder
edge constant, or \code{vals[length(vals)]} for the repeat-last convention).}
}
\value{
\code{list(value, consistency, proportion)}.
}
\description{
The Kirby-style scoring core shared by the MCQ scorers (overall +
magnitude ladders) and the PDQ block scorer. Extracted verbatim from
\code{score_one_mcq()}'s magnitude loop; reproduces Gray et al.'s (2016)
published PDQ lookup tables for all 1024 response patterns per block.
}
\keyword{internal}
