% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-power.R
\name{find_n_discounting}
\alias{find_n_discounting}
\title{Find the smallest sample size reaching a target power (discounting)}
\usage{
find_n_discounting(
  target_power = 0.8,
  effect = list(delta_k = NULL),
  design = list(),
  n_range = c(6, 200),
  n_sim = 200,
  n_sim_max = 4 * n_sim,
  alpha = 0.05,
  df = NULL,
  seed = NULL,
  equation = c("mazur", "exponential"),
  family = c("sltb", "gaussian"),
  random_effects = k ~ 1,
  multi_start = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{target_power}{Target power in (0, 1).}

\item{effect}{Named list supplying \code{delta_k}: the true condition-2 shift
on natural-log k. \code{0} is allowed (useful for Type I error checks).
E.g. \code{delta_k = log(2)} means condition 2's k is twice condition 1's.}

\item{design}{Named list of data-generating settings, merged over the
simulator defaults: \code{delays} (vector), \code{log_k_pop}, \code{sigma_u} (subject
SD on log k), \code{phi} (SLT-beta precision; used when \code{family = "sltb"}),
and \code{sigma_e} (residual SD; used when \code{family = "gaussian"}).}

\item{n_range}{Integer bracket \code{c(lower, upper)} of total N to search
(\verb{2 <= lower < upper}). The search errors (rather than extrapolating)
if the target is not reached at \code{upper}.}

\item{n_sim}{Replicates per evaluation batch. Smaller than the
\code{\link[=power_discounting]{power_discounting()}} default because several N values are evaluated;
the adaptive rule adds batches where the verdict is close.}

\item{n_sim_max}{Maximum replicates per evaluated N (default \code{4 * n_sim}).}

\item{alpha}{Nominal two-sided test level.}

\item{df}{Degrees of freedom for the Wald test's t reference. \code{NULL}
(default) tracks the evaluated sample size as \code{n - 2}; a numeric value
(or \code{Inf} for the asymptotic z-test) is used at every evaluated N.}

\item{seed}{Optional integer seed; identical seeds give identical
results. The caller's RNG state is restored on exit.}

\item{equation}{Discounting function used for BOTH simulation and
refitting (they are always matched): \code{"mazur"} (default) or
\code{"exponential"}. The two-parameter Green-Myerson and Rachlin forms are
out of scope in this version.}

\item{family}{Observation family used for both simulation and refitting:
\code{"sltb"} (default) or \code{"gaussian"}.}

\item{random_effects}{Random-effects formula passed to \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}.
The default \code{k ~ 1} matches the simulator's data-generating process (a
single subject random intercept on log k). The simulator ALWAYS
generates only that intercept: any richer formula (e.g. \code{k + phi ~ 1},
\code{k + s ~ 1}) is accepted but produces a deliberately over-specified
refit of data with no such variance component -- useful for probing
robustness, not for estimating power under those random effects (out
of scope in this version).}

\item{multi_start}{Passed to \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}. Defaults to \code{FALSE} for
speed; non-convergent replicates are excluded and surfaced rather than
biasing the estimate.}

\item{verbose}{Logical; report each evaluation.}

\item{...}{Additional arguments passed to \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}.}
}
\value{
An object of class \code{beezdiscounting_power_n}: a list with
\describe{
\item{n}{Estimated smallest total \code{n_subjects} reaching
\code{target_power}; \code{NA} when the confirmation pass contradicted the
search (\code{status = "unresolved"}).}
\item{target_power}{As supplied.}
\item{status}{\code{"confirmed"} (selected N re-confirmed above target and
N - 1 below), \code{"uncertain"} (a decision relied on a point estimate,
N - 1 also cleared the target on reconfirmation, or an evaluated
lower N read above the target -- so the returned N may not be
minimal), \code{"unresolved"} (the selected N failed its
own reconfirmation; \code{n} is \code{NA}), or \code{"at_lower_bound"} (the target
was already met at \code{n_range[1]} on two independent looks; smaller N
was not explored -- widen \code{n_range} downward if that matters). These
labels describe a heuristic Monte Carlo decision rule -- repeated
looks at ordinary Wilson intervals across several N -- not a formal
sequential error guarantee.}
\item{uncertain}{Logical; \code{TRUE} when any search decision was made on
a point estimate rather than a conclusive Wilson interval, or the
status is not \code{"confirmed"}/\code{"at_lower_bound"}.}
\item{evaluations}{Tibble of every evaluation: \code{n_subjects},
\code{n_sim_total}, \code{n_used}, \code{usable_fraction}, \code{power}, \code{ci_lower},
\code{ci_upper}, \code{decision}. A warning fires when any evaluation had
fewer than 95\% usable fits.}
\item{alpha, df, effect, design, n_range, n_sim, n_sim_max, seed,
settings, call}{Echoed inputs and effective settings.}
}
}
\description{
Bisection search over \code{n_subjects} for the smallest total N whose Monte
Carlo power estimate from \code{\link[=power_discounting]{power_discounting()}} reaches \code{target_power}.
The search accounts for Monte Carlo noise: at each evaluated N,
replicates are added in batches (up to \code{n_sim_max}) until the Wilson
interval for power lies wholly above or below the target; if it still
straddles the target at the cap, the decision falls back to the point
estimate and the result is flagged \code{uncertain}. The selected N and its
lower neighbor are then re-evaluated with fresh replicates before
minimality is claimed.

The returned \code{n} is an \emph{estimated minimum under Monte Carlo uncertainty},
not an exact bound. For grant-quality reporting, rerun
\code{\link[=power_discounting]{power_discounting()}} at the returned \code{n} with a large \code{n_sim} (2000+)
and report that estimate with its Monte Carlo confidence interval.

\strong{Monotonicity assumption.} Bisection presumes that power is
non-decreasing in \code{n_subjects}. Because every evaluated N is judged from
its own independent replicates (and a convergence-conditioned
denominator), a Monte Carlo fluctuation can make a lower N read "below"
when its true power is above the target, so the search may step past a
lower crossing that it never revisits. Evaluated N that contradict the
assumption (a lower N reading "above" the selected N) demote the status to
\code{"uncertain"}; N that were never evaluated cannot be checked. Widen
\code{n_sim}/\code{n_sim_max} when the reported \code{n} matters. When the target is
already met at \code{n_range[1]}, that bound is likewise re-evaluated with
fresh replicates before \code{"at_lower_bound"} is reported; if the second look
does not clear the target the bound is treated as below and the bisection
proceeds upward.
}
\examples{
\donttest{
# Small search for demonstration (use larger n_sim for real planning)
res <- find_n_discounting(
  target_power = 0.8,
  effect = list(delta_k = log(4)),
  n_range = c(4, 40), n_sim = 30, seed = 1, verbose = FALSE
)
print(res)
}

}
\seealso{
\code{\link[=power_discounting]{power_discounting()}} for the Monte Carlo engine.

Other power-analysis:
\code{\link[=power_discounting]{power_discounting()}}
}
\concept{power-analysis}
