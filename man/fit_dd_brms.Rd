% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-brms.R
\name{fit_dd_brms}
\alias{fit_dd_brms}
\title{Fit a Bayesian Mixed-Effects Discounting Model via brms}
\usage{
fit_dd_brms(
  data,
  y_var = "y",
  x_var = "x",
  id_var = "id",
  equation = c("mazur", "exponential", "green-myerson", "rachlin"),
  family = c("beta", "gaussian"),
  boundary = c("error", "squeeze", "zoib"),
  random_effects = k ~ 1,
  covariance_structure = c("pdSymm", "pdDiag"),
  factors = NULL,
  factor_interaction = FALSE,
  continuous_covariates = NULL,
  ll = NULL,
  response_scale = c("proportion", "percent", "amount"),
  prior = NULL,
  autoscale_priors = TRUE,
  chains = 4,
  iter = 2000,
  warmup = floor(iter/2),
  thin = 1,
  cores = getOption("mc.cores", 1L),
  seed = NA,
  backend = getOption("brms.backend", "rstan"),
  control = list(adapt_delta = 0.95),
  init = c("prior_center", "tmb", "random", "0"),
  sample_prior = "no",
  loo = TRUE,
  file = NULL,
  file_refit = getOption("brms.file_refit", "on_change"),
  verbose = 1,
  ...
)
}
\arguments{
\item{data}{Long-format data frame (one row per subject-delay).}

\item{y_var, x_var, id_var}{Column names (canonical defaults), as in
\code{\link[=fit_dd_tmb]{fit_dd_tmb()}}.}

\item{equation}{Discounting equation.}

\item{family}{\code{"beta"} or \code{"gaussian"}; \code{"sltb"} errors with guidance.}

\item{boundary}{Boundary handling for the beta family: \code{"error"}
(default), \code{"squeeze"}, or \code{"zoib"} (see Details). Ignored for
\code{family = "gaussian"}.}

\item{random_effects}{\code{k ~ 1} (single random intercept on \verb{log k}, the
default) or \code{k + phi ~ 1} (adds a per-subject precision random effect;
beta family only). The latter mirrors \code{fit_dd_tmb(random_effects = k + phi ~ 1)}: phi becomes a predicted distributional parameter on the
log link carrying a subject random intercept.}

\item{covariance_structure}{For \code{k + phi ~ 1}, the \verb{(log k, log phi)}
covariance: \code{"pdSymm"} (default; correlated, the TMB parity choice) or
\code{"pdDiag"} (independent). Ignored for \code{k ~ 1}.}

\item{factors, factor_interaction, continuous_covariates}{Fixed-effect
design on \code{logk}, as in \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}.}

\item{ll, response_scale}{Response coercion, as in \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}.}

\item{prior}{Optional \code{brmsprior}; user rows override the defaults.}

\item{autoscale_priors}{Anchor the \code{logk} prior to the median delay (see
\code{\link[=default_dd_priors]{default_dd_priors()}}). For \code{equation = "rachlin"} this also fits on
the normalised delay \code{x / median(x)} (recorded as
\code{param_info$delay_scale}) so the prior is delay-unit invariant; reported
draws are back-transformed to data-unit k, but the raw \code{brmsfit} draws
of the logk intercept stay normalised.}

\item{chains, iter, warmup, thin, cores, seed, backend, control, sample_prior}{MCMC settings passed to \code{\link[brms:brm]{brms::brm()}}.}

\item{init}{\code{"prior_center"} (default), \code{"tmb"} (a quiet
\code{\link[=fit_dd_tmb]{fit_dd_tmb()}} pre-fit supplies the centers, with prior_center
fallback on failure; the beta family maps to the TMB sltb pre-fit),
\code{"random"}, or \code{"0"}; or a list/function passed through to
\code{\link[brms:brm]{brms::brm()}}.}

\item{loo}{Compute and store \code{\link[brms:loo]{brms::loo()}} at fit time.}

\item{file, file_refit}{Passed to \code{\link[brms:brm]{brms::brm()}} for fit caching.}

\item{verbose}{0 (silent), 1 (messages), 2 (full Stan output).}

\item{...}{Passed through to \code{\link[brms:brm]{brms::brm()}}.}
}
\value{
An object of class \code{beezdiscounting_brms}: \code{model} (posterior
medians/SDs on the estimation scale under TMB names \code{beta_k}/\code{log_s},
plus \code{variance_components}), \code{brmsfit}, \code{subject_pars}
(\code{id}, \code{k}, \code{k_lower}, \code{k_upper}, plus \code{phi}/\code{phi_lower}/\code{phi_upper}
for \code{k + phi ~ 1}), \code{converged} (Rhat < 1.01, no
divergences, bulk ESS >= 400), \code{mcmc_info}, \code{loo}, \code{data},
\code{param_info}, \code{priors}, \code{autoscale_info}.
}
\description{
Fits the four discounting equations of \code{\link[=fit_dd_tmb]{fit_dd_tmb()}} (\code{"mazur"},
\code{"exponential"}, \code{"green-myerson"}, \code{"rachlin"}) as Bayesian nonlinear
mixed-effects models, with the discount rate estimated on the natural-log
scale (\code{logk}, subject random intercept; \code{k ~ 1} by default, or
\code{k + phi ~ 1} to add a per-subject precision random effect) and the
shape exponent (\code{logs}) population-level for the two-parameter equations.
}
\details{
\code{family = "beta"} (default) uses \code{Beta(link = "identity")} with the mean
squished into \verb{(1e-6, 1 - 1e-6)}. This is a different likelihood from the
TMB scale-location-truncated beta (\code{family = "sltb"}, which has no brms
equivalent and errors with this pointer): the ordinary beta density is
undefined at exactly 0 and 1, so its estimates are not expected to match
\code{fit_dd_tmb(family = "sltb")} whenever responses sit at or near the
boundaries. Boundary observations (\code{y} exactly 0 or 1) are handled per
\code{boundary}: \code{"error"} (default) refuses to fit and reports how many there
are; \code{"squeeze"} applies the Smithson-Verkuilen transform
\verb{y* = (y (N - 1) + 0.5) / N} to \strong{every} response (an explicit opt-in: it
moves each value toward 0.5 and floors the response at \code{0.5 / N}, so when
the fitted curve approaches that floor -- long delays, steep discounting --
the squeezed values pull k downward); \code{"zoib"} switches to
\code{zero_one_inflated_beta} (changes the estimand -- k then describes interior
responses only). The fraction of exact-boundary and near-boundary
(\code{y <= 0.01} or \code{y >= 0.99}) responses is stored in
\code{param_info$boundary_info} and printed by \code{summary()}.
\code{family = "gaussian"} matches \code{fit_dd_tmb(family = "gaussian")}
wherever the TMB template's mu clamp into \verb{[1e-6, 1 - 1e-6]} does not
bind (everywhere except extreme decay underflow).
}
\seealso{
\code{\link[=fit_dd_tmb]{fit_dd_tmb()}}; \code{\link[=default_dd_priors]{default_dd_priors()}}.
}
