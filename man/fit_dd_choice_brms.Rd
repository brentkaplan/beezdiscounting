% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-brms-choice.R
\name{fit_dd_choice_brms}
\alias{fit_dd_choice_brms}
\title{Fit a Bayesian Structural Choice Discounting Model via brms}
\usage{
fit_dd_choice_brms(
  data,
  mode = c("structural", "descriptive"),
  id_var = "id",
  ss_var = "ss_amount",
  ll_var = "ll_amount",
  delay_var = "delay",
  choice_var = "choice",
  equation = c("mazur", "exponential"),
  intercept = FALSE,
  prior = NULL,
  autoscale_priors = TRUE,
  chains = 4,
  iter = 2000,
  warmup = floor(iter/2),
  thin = 1,
  cores = getOption("mc.cores", 1L),
  seed = NA,
  backend = getOption("brms.backend", "rstan"),
  control = list(adapt_delta = 0.95),
  init = c("prior_center", "random", "0"),
  sample_prior = "no",
  loo = TRUE,
  file = NULL,
  file_refit = getOption("brms.file_refit", "on_change"),
  verbose = 1,
  ...
)
}
\arguments{
\item{data}{Long-format trial-level data (one row per choice).}

\item{mode}{\code{"structural"} (only mode in v1).}

\item{id_var, ss_var, ll_var, delay_var, choice_var}{Column names, as in
\code{\link[=fit_dd_choice]{fit_dd_choice()}}.}

\item{equation}{\code{"mazur"} or \code{"exponential"}.}

\item{intercept}{Include the logit-scale bias term \code{b0}.}

\item{prior}{Optional \code{brmsprior}; user rows override the defaults.}

\item{autoscale_priors}{Anchor the \code{logk} prior to the median delay (see
\code{\link[=default_dd_priors]{default_dd_priors()}}).}

\item{chains, iter, warmup, thin, cores, seed, backend, control, sample_prior}{MCMC settings passed to \code{\link[brms:brm]{brms::brm()}}.}

\item{init}{\code{"prior_center"} (default), \code{"random"}, or \code{"0"}; or a
list/function passed through to \code{\link[brms:brm]{brms::brm()}}.}

\item{loo}{Compute and store \code{\link[brms:loo.brmsfit]{brms::loo()}} at fit time.}

\item{file, file_refit}{Passed to \code{\link[brms:brm]{brms::brm()}} for fit caching.}

\item{verbose}{0 (silent), 1 (messages), 2 (full Stan output).}

\item{...}{Passed through to \code{\link[brms:brm]{brms::brm()}}.}
}
\value{
An object of class \code{beezdiscounting_choice_brms}. Coefficients
are posterior medians on the estimation scale under the TMB names
(\code{beta_k}, \code{log_gamma}, and \code{beta0} when \code{intercept = TRUE}).
}
\description{
The TMB structural likelihood of \code{\link[=fit_dd_choice]{fit_dd_choice()}}:
\verb{logit P(LL) = [b0] + gamma * ((ll/ss) * D(k, delay) - 1)} with choice
sensitivity \code{gamma = exp(loggamma)} and the discount rate \code{k = exp(logk)} carrying the subject random intercept. With
\code{bernoulli("logit")} the brms nonlinear formula IS the logit, so the
likelihood matches TMB exactly.
}
\details{
v1 implements \code{mode = "structural"} only: the descriptive (Young 2018)
model is a plain logistic GLMM expressible directly with
\code{brms::brm()}.
}
\seealso{
\code{\link[=fit_dd_choice]{fit_dd_choice()}}; \code{\link[=default_dd_choice_priors]{default_dd_choice_priors()}}.
}
