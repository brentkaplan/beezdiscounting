% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{fit_dd_tmb}
\alias{fit_dd_tmb}
\title{Fit an indifference-point mixed-effects discounting model via TMB}
\usage{
fit_dd_tmb(
  data,
  y_var = "y",
  x_var = "x",
  id_var = "id",
  equation = c("mazur", "exponential"),
  family = c("sltb", "gaussian"),
  random_effects = k ~ 1,
  factors = NULL,
  factor_interaction = FALSE,
  continuous_covariates = NULL,
  ll = NULL,
  response_scale = c("proportion", "percent", "amount"),
  start_values = NULL,
  tmb_control = list(iter_max = 1000, eval_max = 2000),
  multi_start = TRUE,
  verbose = 1,
  ...
)
}
\arguments{
\item{data}{Long data frame with subject id, delay, and indifference
proportion columns.}

\item{y_var, x_var, id_var}{Column names (defaults \code{"y"}, \code{"x"}, \code{"id"}).}

\item{equation}{One of \code{"mazur"}, \code{"exponential"}.}

\item{family}{Observation family: \code{"sltb"} (default) or \code{"gaussian"}.}

\item{random_effects}{RE formula (MVP: \code{k ~ 1}).}

\item{factors}{Character vector of between-subject factor names.}

\item{factor_interaction}{Logical; include a pairwise factor interaction.}

\item{continuous_covariates}{Character vector of covariate names.}

\item{ll}{Optional larger-later reward for \code{amount}-scale coercion.}

\item{response_scale}{One of \code{"proportion"}, \code{"percent"}, \code{"amount"}.}

\item{start_values}{Optional named list overriding defaults.}

\item{tmb_control}{Optimizer control list.}

\item{multi_start}{Logical; if \code{TRUE} (default), run the 3-set guarded
multi-start.}

\item{verbose}{Integer verbosity (0 silent, 1 progress, 2 debug).}

\item{...}{Reserved.}
}
\value{
An object of class \code{beezdiscounting_tmb} with components:
\describe{
\item{call}{The matched call.}
\item{opt}{Normalized optimizer result (\code{par}, \code{objective},
\code{convergence}, \code{message}).}
\item{model}{List of \code{coefficients}, \code{se}, and \code{variance_components}.}
\item{sdr}{TMB \code{sdreport} object (or \code{NULL} if SE computation failed).}
\item{hessian_pd}{Logical positive-definiteness of the Hessian.}
\item{param_info}{Model metadata (equation, family, dimensions, factor
spec, parsed random effects).}
\item{formula_details}{Fixed-effect design (\code{X}, \code{rhs}, \code{contrasts}).}
\item{subject_pars}{Data frame of subject-level \code{id}, \code{u_i}, \code{k}.}
\item{loglik, AIC, BIC}{Fit statistics.}
\item{converged, se_available}{Convergence / SE-availability flags.}
\item{opt_warnings}{Character vector of optimizer warnings.}
\item{data}{The single filtered model frame (id/x/y + retained design
columns), row-aligned with the design matrix.}
\item{data_all}{The validated frame before complete-casing.}
\item{coercion_info}{Scale-coercion/clamping audit list.}
}
}
\description{
Fits a 1-parameter discounting model (Mazur hyperbolic or exponential) with a
random intercept on \verb{log k}, between-subject fixed effects, and either an
SLT-beta or Gaussian observation family, using Template Model Builder for
exact AD + Laplace approximation.
}
\examples{
\donttest{
# Small two-subject long-format indifference-point data frame.
dd <- data.frame(
  id = rep(c("s1", "s2"), each = 5),
  x  = rep(c(7, 30, 180, 365, 730), times = 2),
  y  = c(0.95, 0.80, 0.45, 0.30, 0.15,
         0.90, 0.70, 0.40, 0.25, 0.10)
)
fit <- fit_dd_tmb(dd, equation = "mazur", family = "sltb", verbose = 0)
exp(fit$model$coefficients[["beta_k"]])  # population k
}
}
