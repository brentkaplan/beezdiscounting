% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb.R
\name{fit_dd_tmb}
\alias{fit_dd_tmb}
\title{Fit an indifference-point mixed-effects discounting model via TMB}
\usage{
fit_dd_tmb(
  data,
  y_var = "y",
  x_var = "x",
  id_var = "id",
  equation = c("mazur", "exponential", "green-myerson", "rachlin"),
  family = c("sltb", "gaussian"),
  random_effects = k ~ 1,
  factors = NULL,
  factor_interaction = FALSE,
  continuous_covariates = NULL,
  ll = NULL,
  response_scale = c("proportion", "percent", "amount"),
  start_values = NULL,
  tmb_control = list(iter_max = 1000, eval_max = 2000),
  multi_start = TRUE,
  verbose = 1,
  covariance_structure = c("pdSymm", "pdDiag"),
  ...
)
}
\arguments{
\item{data}{Long data frame with subject id, delay, and indifference
proportion columns.}

\item{y_var, x_var, id_var}{Column names (defaults \code{"y"}, \code{"x"}, \code{"id"}).}

\item{equation}{One of \code{"mazur"}, \code{"exponential"}, \code{"green-myerson"}, or
\code{"rachlin"}. The two 2-parameter (hyperboloid) forms add a single
population nonlinearity exponent \code{s} (estimated on the log scale) and
reduce to \code{"mazur"} at \code{s = 1}.}

\item{family}{Observation family: \code{"sltb"} (default) or \code{"gaussian"}.
For \code{"sltb"}, responses outside \verb{[0, 1]} after scaling are clamped (with a
warning); for \code{"gaussian"}, whose likelihood is unbounded, they are kept
as observed and percent-scaled data are not detected automatically (set
\code{response_scale = "percent"}).}

\item{random_effects}{RE formula: \code{k ~ 1} (single random intercept on
\verb{log k}), \code{k + phi ~ 1} (a joint 2-D random intercept on
\verb{(log k, log phi)}, SLT-beta only), or \code{k + s ~ 1} (a joint 2-D random
intercept on \verb{(log k, log s)}, Green-Myerson and Rachlin only).}

\item{factors}{Character vector of between-subject factor names.}

\item{factor_interaction}{Logical; include a pairwise factor interaction.}

\item{continuous_covariates}{Character vector of covariate names.}

\item{ll}{Optional larger-later reward for \code{amount}-scale coercion.}

\item{response_scale}{One of \code{"proportion"}, \code{"percent"}, \code{"amount"}.}

\item{start_values}{Optional named list overriding defaults.}

\item{tmb_control}{Optimizer control list.}

\item{multi_start}{Logical; if \code{TRUE} (default), run the 3-set guarded
multi-start. Converged starts are preferred over non-converged ones,
then the lowest negative log-likelihood wins; the Hessian is checked on
the kept fit only.}

\item{verbose}{Integer verbosity (0 silent, 1 progress, 2 debug).}

\item{covariance_structure}{Covariance for a 2-D random effect
(\code{k + phi ~ 1} or \code{k + s ~ 1}): \code{"pdSymm"} (default; correlated random
intercepts) or \code{"pdDiag"} (independent, correlation fixed at 0). Ignored
for \code{k ~ 1}.}

\item{...}{Reserved.}
}
\value{
An object of class \code{beezdiscounting_tmb} with components:
\describe{
\item{call}{The matched call.}
\item{opt}{Normalized optimizer result (\code{par}, \code{objective},
\code{convergence}, \code{message}).}
\item{model}{List of \code{coefficients}, \code{se}, and \code{variance_components}.}
\item{sdr}{TMB \code{sdreport} object (or \code{NULL} if SE computation failed).}
\item{hessian_pd}{Logical positive-definiteness of the Hessian.}
\item{param_info}{Model metadata (equation, family, dimensions, factor
spec, parsed random effects).}
\item{formula_details}{Fixed-effect design (\code{X}, \code{rhs}, \code{contrasts}).}
\item{subject_pars}{Data frame of subject-level parameters. For a 1-RE fit
(\code{k ~ 1}) the columns are \verb{id, u_i, k}; for a phi-target 2-RE fit
(\code{k + phi ~ 1}) they are \verb{id, re_k, re_phi, k, phi}; for an s-target
2-RE fit (\code{k + s ~ 1}, GM/Rachlin) they are \verb{id, re_k, re_s, k, s}
where \code{s} is soft-clamped toward \verb{(0.05, 20)}.}
\item{loglik, AIC, BIC}{Fit statistics.}
\item{converged, se_available}{Convergence / SE-availability flags. A
non-converged fit raises a \code{beezdiscounting_convergence_warning} at
fit time (regardless of \code{verbose}) and again from \code{tidy()},
\code{confint()}, \code{summary()}, \code{\link[=get_dd_param_emms]{get_dd_param_emms()}} and
\code{\link[=get_dd_comparisons]{get_dd_comparisons()}}.}
\item{multi_start_info}{List recording the start selection:
\code{n_starts}, \code{n_finite}, \code{n_converged}, \code{selected_start}, \code{tier}
(1 = converged and passing the log-k sanity guard, 2 = passing the
guard but not converged, 3 = neither), and \code{lower_nll_nonconverged}
(\code{TRUE} when a non-converged start reached a lower NLL than the kept
one).}
\item{opt_warnings}{Character vector of optimizer warnings.}
\item{data}{The single filtered model frame (id/x/y + retained design
columns), row-aligned with the design matrix.}
\item{data_all}{The validated frame before complete-casing.}
\item{coercion_info}{Scale-coercion/clamping audit list.}
}
}
\description{
Fits a discounting model (Mazur hyperbolic, exponential, or the
two-parameter Green-Myerson / Rachlin hyperboloids) with a random intercept
on \verb{log k}, between-subject fixed effects, and either an SLT-beta or
Gaussian observation family, using Template Model Builder for exact AD +
Laplace approximation.
}
\section{Scales, guards and floors}{

\itemize{
\item \strong{Random-effect scales.} \code{\link[nlme:VarCorr]{nlme::VarCorr()}} reports the subject SD of \verb{log k}
on the natural-log scale (\code{Term = "k"} means log k); \code{\link[=tidy]{tidy()}} and
\code{\link[=summary]{summary()}} convert it to the log10 scale and label it so. \code{\link[=ranef]{ranef()}}
returns the standardised deviate \code{u_i} for a \code{k ~ 1} fit but
natural-log offsets (\code{re_k}, \code{re_phi} / \code{re_s}) for a two-random-effect
fit.
\item \strong{Mean guard.} The fitted mean is held inside \verb{[1e-6, 1 - 1e-6]}. For
the exponential equation this binds once \code{k * delay} exceeds about
13.8 (for \code{k = 0.01}, delays beyond roughly 1,400 days); such
observations carry no information about \code{k}, so very steep discounters
measured at long delays lose curvature. The hyperbolic forms are
essentially unaffected.
\item \strong{Shape \code{s}.} The reported \code{s} is the unclamped population value
\code{exp(log_s)}. With \code{k + s ~ 1} each subject's effective \code{s} is
soft-clamped into \verb{(0.05, 20)}, and the \code{VarCorr()} SD of \code{s} is on the
latent (pre-clamp) log scale.
\item \strong{Precision floor.} SLT-beta precision is bounded below at
\code{phi = 0.1}. For \code{k ~ 1} this is an optimizer bound that
\code{tmb_control$lower} can relax; with \code{k + phi ~ 1} each subject's \code{phi}
is floored at 0.1 inside the likelihood and cannot be relaxed.
}
}

\section{Two-parameter equations (Green-Myerson, Rachlin)}{

\code{k} and \code{s} trade off against each other, and with the few delays of a
typical titration task (about 7) their maximum-likelihood estimates are
biased at realistic sample sizes. In simulation (N = 80 to 400 subjects,
7 delays) \verb{log k} came out low by roughly 0.1 to 0.25 and \code{s} high by
up to about 0.2, with small biases in the random-effect SDs for
\code{k + s ~ 1}. This is a property of the estimator with this design, not of
the code: an independent likelihood reproduces the same estimates. The
pair is also sensitive to how responses near zero are recorded. The SLT-beta
log-density grows steeply as a response approaches 0, so the same data
rounded to 3 decimals, or floored at 0.001, can move \verb{(log k, s)} by
several tenths; Mazur fits to the same data move very little. Report the
recording resolution of the indifference points, keep it identical
across groups being compared, and treat \verb{(k, s)} from these equations as
less stable than a one-parameter \code{k}.
}

\examples{
\donttest{
# Small two-subject long-format indifference-point data frame.
dd <- data.frame(
  id = rep(c("s1", "s2"), each = 5),
  x  = rep(c(7, 30, 180, 365, 730), times = 2),
  y  = c(0.95, 0.80, 0.45, 0.30, 0.15,
         0.90, 0.70, 0.40, 0.25, 0.10)
)
fit <- fit_dd_tmb(dd, equation = "mazur", family = "sltb", verbose = 0)
exp(fit$model$coefficients[["beta_k"]])  # population k
}
}
\references{
Young, M. E. (2017). Discounting: A practical guide to multilevel analysis of
indifference data. \emph{Journal of the Experimental Analysis of Behavior,
108}(1), 97-112. \doi{10.1002/jeab.265}

Kim, M., Koffarnus, M. N., & Franck, C. T. (2024). Thinking inside the bounds:
Improved error distributions for indifference point data analysis and simulation
via beta regression using common discounting functions. \emph{arXiv} preprint
arXiv:2404.18000.

Kim, M., Kaplan, B. A., Koffarnus, M. N., & Franck, C. T. (2025).
Scale-location-truncated beta regression: Expanding beta regression to
accommodate 0 and 1. \emph{arXiv} preprint arXiv:2509.13167.
}
