% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdq.R
\name{pdq_to_choice}
\alias{pdq_to_choice}
\title{Convert PDQ responses to a trial-level choice frame}
\usage{
pdq_to_choice(
  responses,
  id_var = "subjectid",
  question_var = "questionid",
  response_var = "response"
)
}
\arguments{
\item{responses}{Long-form data frame with one row per PDQ item per
subject, holding the columns named by \code{id_var}, \code{question_var}, and
\code{response_var}. \code{response} is \code{0} for the smaller guaranteed reward and
\code{1} for the larger risky reward.}

\item{id_var, question_var, response_var}{Column names in \code{responses} for
the subject id, PDQ question id (1-30), and the binary choice. Defaults
match the bundled \code{pdq} dataset (\code{"subjectid"}, \code{"questionid"},
\code{"response"}).}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{id} (character),
\code{sc_amount}, \code{lu_amount}, \code{prob}, \code{theta} (odds against winning), and
\code{choice} (\code{0}/\code{1}, \code{1} = chose the risky reward), in the input row
order.
}
\description{
Reshapes long-form Probability Discounting Questionnaire (PDQ) responses
into a per-trial certain-versus-risky choice frame, joining each
\code{questionid} to the canonical item design (certain amount, probabilistic
amount, probability, and odds against \code{theta = (1 - p)/p}) from
Madden, Petry, & Johnson (2009; see \code{\link[=get_lookup_table]{get_lookup_table()}}).
}
\details{
Unknown or non-coercible question ids raise an error rather
than silently producing unmatched rows. Ragged input is allowed --
subjects need not have all items -- and \code{NA} responses are preserved.
For the strict scorer see \code{\link[=score_pdq]{score_pdq()}}.
}
\examples{
ch <- pdq_to_choice(pdq)
head(ch)
}
\seealso{
\code{\link[=score_pdq]{score_pdq()}}, \code{\link[=get_lookup_table]{get_lookup_table()}}, \code{\link[=mcq_to_choice]{mcq_to_choice()}}
}
