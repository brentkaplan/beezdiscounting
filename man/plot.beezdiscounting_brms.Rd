% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-plot.R
\name{plot.beezdiscounting_brms}
\alias{plot.beezdiscounting_brms}
\title{Plot a Bayesian (brms) indifference-point discounting model}
\usage{
\method{plot}{beezdiscounting_brms}(
  x,
  type = c("population", "individual", "parameters", "resid"),
  ids = NULL,
  at = NULL,
  n_points = 200,
  x_trans = c("log10", "linear"),
  show_observed = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{beezdiscounting_brms} object.}

\item{type}{One of \code{"population"}, \code{"individual"}, \code{"parameters"}, \code{"resid"}.}

\item{ids}{Optional subset of subject ids for \code{type = "individual"}.}

\item{at}{Optional named list conditioning the population curve on factor
levels / covariate values.}

\item{n_points}{Number of delay points in the curve grid.}

\item{x_trans}{Delay-axis scale: \code{"log10"} (default) or \code{"linear"}.}

\item{show_observed}{Overlay the observed indifference points.}

\item{...}{Unused.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Visualize a fitted \code{\link[=fit_dd_brms]{fit_dd_brms()}} model. \code{type = "population"} (default)
draws the posterior-median discount curve with a credible band for the
population conditional mean (random effects set to zero -- a credible band,
not a posterior-predictive band) over the observed (boundary-squeezed)
indifference points; \code{type = "individual"} adds per-subject median curves;
\code{type = "parameters"} shows the posterior subject-\code{k} caterpillar;
\code{type = "resid"} plots standardized residuals against fitted values.
}
