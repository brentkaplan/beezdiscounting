% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-plot.R
\name{plot.beezdiscounting_choice}
\alias{plot.beezdiscounting_choice}
\title{Plot a choice-based discounting model}
\usage{
\method{plot}{beezdiscounting_choice}(
  x,
  type = NULL,
  ids = NULL,
  at = NULL,
  n_points = 200,
  x_trans = c("log10", "linear"),
  show_observed = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{beezdiscounting_choice} object.}

\item{type}{One of \code{"population"}, \code{"individual"}, \code{"calibration"},
\code{"parameters"}. Defaults to \code{"population"} (structural) or \code{"calibration"}
(descriptive).}

\item{ids}{Optional subset of subject ids for \code{type = "individual"}.}

\item{at}{Optional named list conditioning the curve on factor levels /
covariate values.}

\item{n_points}{Number of delay points in the curve grid.}

\item{x_trans}{Delay-axis scale: \code{"log10"} (default) or \code{"linear"}.}

\item{show_observed}{Unused for the implied discount curve (kept for a
consistent signature across tiers).}

\item{...}{Unused.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Visualize a fitted \code{\link[=fit_dd_choice]{fit_dd_choice()}} model. For a \emph{structural} fit,
\code{type = "population"} (default) draws the implied discount curve from the
estimated discount rate, \code{type = "individual"} adds the per-subject curves,
and \code{type = "parameters"} shows the subject-\code{k} distribution. For both
structural and \emph{descriptive} (Young 2018) fits, \code{type = "calibration"} plots
the observed choice proportion against the fitted P(choose LL), binned by
fitted probability (a likelihood-geometry-agnostic goodness-of-fit view).
Descriptive fits have no structural discount rate, so the curve/parameter
types are unavailable.
}
\examples{
\donttest{
ch <- simulate_dd_choice(n_subjects = 12, seed = 1)
fit <- fit_dd_choice(ch, equation = "mazur")
plot(fit)
plot(fit, type = "calibration")
}
}
