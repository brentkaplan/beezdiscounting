% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-plot.R
\name{plot.beezdiscounting_comparison}
\alias{plot.beezdiscounting_comparison}
\title{Plot group differences in discount rate}
\usage{
\method{plot}{beezdiscounting_comparison}(x, type = c("ratio", "difference"), ...)
}
\arguments{
\item{x}{A \code{beezdiscounting_comparison} object.}

\item{type}{\code{"ratio"} (default) or \code{"difference"}.}

\item{...}{Unused.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Forest plot of the pairwise (or treatment-vs-control) contrasts from
\code{\link[=get_dd_comparisons]{get_dd_comparisons()}}. \code{type = "ratio"} (default) shows each k ratio on a
log axis with a reference line at 1; \code{type = "difference"} shows the
difference in log10 k with a reference line at 0. A contrast is flagged when
its interval excludes the null -- a backend-agnostic encoding that works for
both the frequentist (TMB) and Bayesian (brms) backends.
}
\examples{
\donttest{
sim <- simulate_dd_ip(n_subjects = 30, n_conditions = 2, delta_k = c(0, 0.5), seed = 1)
fit <- fit_dd_tmb(sim, equation = "mazur", factors = "condition")
cmp <- get_dd_comparisons(fit)
plot(cmp)
}
}
