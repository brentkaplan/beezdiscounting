% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-plot.R
\name{plot.beezdiscounting_tmb}
\alias{plot.beezdiscounting_tmb}
\title{Plot a mixed-effects discounting model}
\usage{
\method{plot}{beezdiscounting_tmb}(
  x,
  type = c("population", "individual", "parameters", "resid"),
  ids = NULL,
  at = NULL,
  n_points = 200,
  x_trans = c("log10", "linear"),
  show_observed = TRUE,
  ...
)
}
\arguments{
\item{x}{A \code{beezdiscounting_tmb} object.}

\item{type}{One of \code{"population"}, \code{"individual"}, \code{"parameters"}, \code{"resid"}.}

\item{ids}{Optional subset of subject ids for \code{type = "individual"}.}

\item{at}{Optional named list conditioning the population curve on factor
levels / covariate values (e.g. \code{list(group = "A")}), passed to the same
reference-grid machinery as \code{\link[=get_dd_comparisons]{get_dd_comparisons()}}.}

\item{n_points}{Number of delay points in the curve grid.}

\item{x_trans}{Delay-axis scale: \code{"log10"} (default) or \code{"linear"}.}

\item{show_observed}{Overlay the observed indifference points.}

\item{...}{Unused.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
Visualize a fitted \code{\link[=fit_dd_tmb]{fit_dd_tmb()}} model. \code{type = "population"} draws the
population (random-effects-at-zero) discount curve over the observed
indifference points; \code{type = "individual"} adds the per-subject curves (the
shrinkage picture). For a fit with factors the population curve is drawn once
per factor-level combination; use \code{at} to condition on specific levels or
covariate values. \code{type = "parameters"} shows the distribution of the
subject-specific discount rate \code{k} (log scale); \code{type = "resid"} plots
standardized (Pearson) residuals against fitted values.
}
\examples{
\donttest{
sim <- simulate_dd_ip(n_subjects = 12, seed = 1)
fit <- fit_dd_tmb(sim, equation = "mazur")
plot(fit)
plot(fit, type = "individual")
}
}
