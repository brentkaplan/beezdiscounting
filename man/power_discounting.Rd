% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-power.R
\name{power_discounting}
\alias{power_discounting}
\title{Monte Carlo power analysis for between-subject discounting designs}
\usage{
power_discounting(
  n_subjects,
  effect = list(delta_k = NULL),
  design = list(),
  n_sim = 500,
  alpha = 0.05,
  df = NULL,
  seed = NULL,
  equation = c("mazur", "exponential"),
  family = c("sltb", "gaussian"),
  random_effects = k ~ 1,
  multi_start = FALSE,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{n_subjects}{Total number of simulated subjects per replicate, split
across the two conditions round-robin (even numbers give equal groups).}

\item{effect}{Named list supplying \code{delta_k}: the true condition-2 shift
on natural-log k. \code{0} is allowed (useful for Type I error checks).
E.g. \code{delta_k = log(2)} means condition 2's k is twice condition 1's.}

\item{design}{Named list of data-generating settings, merged over the
simulator defaults: \code{delays} (vector), \code{log_k_pop}, \code{sigma_u} (subject
SD on log k), \code{phi} (SLT-beta precision; used when \code{family = "sltb"}),
and \code{sigma_e} (residual SD; used when \code{family = "gaussian"}).}

\item{n_sim}{Number of Monte Carlo replicates. 500 (default) is suitable
for interactive exploration; use 2000+ for grant-quality precision (see
\code{vignette("power-analysis")}).}

\item{alpha}{Nominal two-sided test level.}

\item{df}{Degrees of freedom for the Wald test's t reference
distribution. \code{NULL} (default) uses \code{n_subjects - 2}, the two-sample df
of the between-subject design. This is an \emph{empirically calibrated}
small-sample correction, not a model-derived df (the TMB fit has no
exact t sampling theory); it passes the package's Type I calibration
battery, while the asymptotic z-test (\code{df = Inf}) is anticonservative
at study-relevant sample sizes.}

\item{seed}{Optional integer seed; identical seeds give identical
results. The caller's RNG state is restored on exit.}

\item{equation}{Discounting function used for BOTH simulation and
refitting (they are always matched): \code{"mazur"} (default) or
\code{"exponential"}. The two-parameter Green-Myerson and Rachlin forms are
out of scope in this version.}

\item{family}{Observation family used for both simulation and refitting:
\code{"sltb"} (default) or \code{"gaussian"}.}

\item{random_effects}{Random-effects formula passed to \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}.
The default \code{k ~ 1} matches the simulator's data-generating process (a
single subject random intercept on log k). The simulator ALWAYS
generates only that intercept: any richer formula (e.g. \code{k + phi ~ 1},
\code{k + s ~ 1}) is accepted but produces a deliberately over-specified
refit of data with no such variance component -- useful for probing
robustness, not for estimating power under those random effects (out
of scope in this version).}

\item{multi_start}{Passed to \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}. Defaults to \code{FALSE} for
speed; non-convergent replicates are excluded and surfaced rather than
biasing the estimate.}

\item{verbose}{Logical; show a progress bar.}

\item{...}{Additional arguments passed to \code{\link[=fit_dd_tmb]{fit_dd_tmb()}} (e.g.
\code{tmb_control}).}
}
\value{
An object of class \code{beezdiscounting_power}: a list with
\describe{
\item{power}{Estimated power: proportion of usable replicates whose
Wald CI excludes 0 (equal to \code{hit_rate_ci}). \code{NA} if no replicate
was usable.}
\item{power_mc_ci}{Wilson 95\% confidence interval on \code{power},
reflecting Monte Carlo uncertainty from \code{n_used} replicates.}
\item{hit_rate_p}{Proportion of usable replicates with \code{p < alpha}.}
\item{hit_rate_ci}{Proportion of usable replicates whose Wald CI
excludes 0 (the same decision rule as \code{hit_rate_p}, since both use
the same SE and t reference; both reported).}
\item{n_sim}{Total replicates attempted.}
\item{n_converged}{Replicates whose fit converged.}
\item{n_hessian_pd}{Replicates with a positive-definite Hessian.}
\item{n_used}{Replicates entering the power denominator (converged,
positive-definite Hessian, finite SE).}
\item{alpha}{Nominal test level.}
\item{df}{Degrees of freedom of the t reference distribution actually
used (\code{n_subjects - 2} unless overridden).}
\item{effect}{The validated effect specification (name and delta).}
\item{target_term}{The tested coefficient (\code{"k:conditionC2"}).}
\item{design}{The merged design list actually used.}
\item{n_subjects}{As supplied.}
\item{replicates}{Tibble with one row per replicate: \code{sim}, \code{status}
(\code{"ok"}, \code{"nonconverged"}, \code{"hessian_not_pd"}, \code{"se_unusable"},
\code{"error"}), \code{converged}, \code{hessian_pd}, \code{estimate}, \code{se},
\code{statistic}, \code{p_value}, \code{ci_lower}, \code{ci_upper}, \code{hit_p}, \code{hit_ci},
and \code{message} (error text, if any). Estimates are on the
natural-log-k scale of the simulated \code{delta_k}.}
\item{seed}{As supplied.}
\item{settings}{List of \code{equation}, \code{family}, \code{multi_start}, and the
deparsed random-effects formula.}
\item{call}{The matched call.}
}
}
\description{
Estimates statistical power to detect a between-subject difference in the
discount rate (a shift \code{delta_k} on natural-log k) by simulation: each
replicate (1) simulates a two-condition between-subject indifference-point
dataset with \code{\link[=simulate_dd_ip]{simulate_dd_ip()}} under assumed population parameters plus
the effect, (2) refits it with \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}, and (3) tests the log-k
condition contrast with a Wald test at level \code{alpha}, referred to a t
distribution with \code{df} degrees of freedom (see the \code{df} argument). Power
is the proportion of \emph{usable} fits (converged, positive-definite Hessian,
finite standard error) that reject.

Because the power estimate is a proportion from finitely many replicates,
it is reported with a Wilson score confidence interval (\code{power_mc_ci}).
Both a p-value verdict (\code{p < alpha}) and a confidence-interval verdict
(Wald CI excludes 0) are recorded per replicate; they use the same
standard error and reference distribution, so they coincide by
construction, and both rates are returned.

This mirrors \code{beezdemand::power_demand()}. The design asymmetry is
intentional: this package's simulator models a \emph{between-subject} condition
(subjects are split across conditions round-robin), while beezdemand's
models a within-subject condition.
}
\details{
A replicate whose fit fails (non-convergence, non-positive-definite
Hessian, unusable standard error, or an error) is excluded from the power
denominator and reported through the \verb{n_*} counts and
\verb{$replicates$status} -- it is never counted as "no effect detected", which
would bias power in an unpredictable direction. A warning is issued when
fewer than 95\% of replicates are usable.

The v1 scope is a single fixed-effect \code{delta_k} under the package's
existing between-subject simulator. Effects on \code{s} or \code{phi}, power for
derived measures (ED50, AUC), and arbitrary designs are out of scope; see
\code{vignette("power-analysis")}.
}
\examples{
\donttest{
# Quick exploratory run (use n_sim >= 500 for real planning)
res <- power_discounting(
  n_subjects = 40,
  effect = list(delta_k = log(2)),
  n_sim = 20, seed = 1, verbose = FALSE
)
print(res)
}

}
\seealso{
\code{\link[=find_n_discounting]{find_n_discounting()}} to search for the smallest adequate
sample size; \code{\link[=fit_dd_tmb]{fit_dd_tmb()}} for the model being refit;
\code{\link[=simulate_dd_ip]{simulate_dd_ip()}} for the data-generating process.

Other power-analysis:
\code{\link[=find_n_discounting]{find_n_discounting()}}
}
\concept{power-analysis}
