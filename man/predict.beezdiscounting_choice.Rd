% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-choice-methods.R
\name{predict.beezdiscounting_choice}
\alias{predict.beezdiscounting_choice}
\title{Predict from a structural choice discounting model}
\usage{
\method{predict}{beezdiscounting_choice}(
  object,
  newdata = NULL,
  type = c("prob", "parameters"),
  level = "subject",
  ...
)
}
\arguments{
\item{object}{A \code{beezdiscounting_choice} object.}

\item{newdata}{Optional data frame. \code{NULL} uses the fitting data. A supplied
\code{newdata} must use the package's canonical column names (\code{id}, \code{ss_amount},
\code{ll_amount}, \code{delay}, plus any factor/covariate columns).}

\item{type}{\code{"prob"} (default; fitted choice probabilities P(LL) on \verb{(0,1)})
or \code{"parameters"} (the per-subject parameter tibble).}

\item{level}{For \code{type = "prob"}: \code{"subject"} (default; conditions on each
subject's estimated random intercept, requires the id column) or
\code{"population"} (random effects set to zero - the population-mean curve; no
id column needed).}

\item{...}{Unused.}
}
\value{
\itemize{
\item \code{type = "parameters"}: the subject-parameter tibble (one row per subject).
\item \code{type = "prob"}: \code{newdata} as a tibble plus a \code{.prob} column of fitted
choice probabilities.
}
}
\description{
Predict from a structural choice discounting model
}
