% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb-methods.R
\name{predict.beezdiscounting_tmb}
\alias{predict.beezdiscounting_tmb}
\title{Predict from a TMB mixed-effects discounting model}
\usage{
\method{predict}{beezdiscounting_tmb}(
  object,
  newdata = NULL,
  type = c("response", "parameters"),
  level = "subject",
  ...
)
}
\arguments{
\item{object}{A \code{beezdiscounting_tmb} object.}

\item{newdata}{Optional data frame. \code{NULL} uses the fitting data. A supplied
\code{newdata} must use the package's canonical column names (\code{id}, \code{x}, \code{y}),
regardless of the \code{id_var}/\code{x_var}/\code{y_var} you gave at fit time.}

\item{type}{\code{"response"} (fitted indifference proportions on \verb{(0,1)}) or
\code{"parameters"} (the per-subject parameter tibble).}

\item{level}{For \code{type = "response"}: \code{"subject"} (default; conditions on
each subject's estimated random intercept, requires the id column) and/or
\code{"population"} (random effects set to zero - the population-mean curve;
no id column needed). Pass \code{c("population", "subject")} for both columns
side-by-side. A numeric nlme-style level is rejected with an error. For
an s-target 2-RE fit (\code{k + s ~ 1}), the subject level uses each subject's
estimated \code{s_i}; the population level (random effects zero) uses the
soft-clamped population \code{s = .dd_soft_clamp_s_log(log_s)}, matching the
kernel near the \verb{(0.05, 20)} bounds (equal to \code{exp(log_s)} in the interior).}

\item{...}{Unused.}
}
\value{
\itemize{
\item \code{type = "parameters"}: the subject-parameter tibble (one row per subject).
\item \code{type = "response"}, \code{level = "subject"} only: \code{newdata} as a tibble plus
a \code{.fitted} column.
\item \code{type = "response"}, \code{"population"} requested: \code{newdata} plus
\code{predict.fixed} (and \code{predict.id} when \code{"subject"} is also requested),
matching the \code{nlme::predict.lme(level = 0:1)} column-name convention so
nlme-based plotting code runs unchanged.
}
}
\description{
Predict from a TMB mixed-effects discounting model
}
\examples{
\donttest{
dd <- simulate_dd_ip(n_subjects = 20, seed = 1)
fit <- fit_dd_tmb(dd, equation = "mazur", family = "sltb",
                  random_effects = k ~ 1, verbose = 0)

# Subject-conditional fitted values
head(predict(fit, type = "response"))

# Population-mean curve at specific delays (no id column needed)
nd <- data.frame(x = c(7, 30, 180, 365))
predict(fit, newdata = nd, level = "population")

# Per-subject parameters
head(predict(fit, type = "parameters"))
}

}
