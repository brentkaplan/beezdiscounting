% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdq.R
\name{prop_sc}
\alias{prop_sc}
\title{Calculate proportion of guaranteed choices at each PDQ h rank}
\usage{
prop_sc(dat)
}
\arguments{
\item{dat}{Dataframe (longform) with subjectid, questionid (1-30), and
response (0 for the guaranteed reward and 1 for the risky reward)}
}
\value{
Dataframe with proportion of guaranteed choices at each h rank
}
\description{
The PDQ analog of \code{\link[=prop_ss]{prop_ss()}}: pools all subjects' responses and
reports, for each of the 10 h ranks (one item per block per rank), the
proportion choosing the smaller guaranteed reward. \code{1 - prop_sc} is the
risky-choice proportion at that rank.
}
\examples{
prop_sc(pdq)
}
