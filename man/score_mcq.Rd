% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mcq.R
\name{score_mcq}
\alias{score_mcq}
\title{Score an MCQ (27- or 21-item)}
\usage{
score_mcq(
  dat = dat,
  items = 27,
  impute_method = "none",
  round = 6,
  random = FALSE,
  trans = "none",
  return_data = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{dat}{Dataframe (longform) with subjectid, questionid, and response
(0 for SIR/SS and 1 for LDR/LL)}

\item{items}{Number of MCQ items: 27 (Kirby, Petry, & Bickel, 1999) or 21
(Kirby & Marakovic, 1996). Default is 27.}

\item{impute_method}{One of: "none", "ggm", "GGM", "inn", "INN"}

\item{round}{Numeric specifying number of decimal places
(passed to \code{base::round()})}

\item{random}{Boolean whether to insert a random draw (0 or 1) for NAs.
Default is FALSE}

\item{trans}{Transformation to apply to k values: "none", "log", or "ln".
Default is "none"}

\item{return_data}{Boolean whether to return the original data and new
imputed responses. Default is FALSE.}

\item{verbose}{Boolean whether to print subject and question ids pertaining
to missing data. Default is FALSE.}
}
\value{
If \code{return_data = FALSE} (default), a summary data frame with one
row per subject. If \code{return_data = TRUE}, a list with \code{results} (that
summary data frame) and \code{data} (the input data plus a \code{newresponse}
column reflecting any imputation).
}
\description{
Score an MCQ (27- or 21-item)
}
\details{
Each subject's data must satisfy a strict contract: exactly one
row per canonical question id (\code{items} of them; no duplicates, no unknown
ids, none missing) and responses coded 0, 1, or \code{NA} (numeric, logical, or
character/factor values that coerce to 0/1). Malformed input errors
rather than silently mis-scoring. Contrast with \code{\link[=mcq_to_choice]{mcq_to_choice()}}'s
lenient, ragged contract, which accepts partial per-subject coverage.

Ladder edges follow the Kaplan et al. (2014) Excel scorers. The overall
(all-item) ladder appends a fixed edge value past the steepest item:
0.25 for the 27-item MCQ and 0.1333 for the 21-item MCQ. The small /
medium / large magnitude ladders instead repeat their own last k. A
respondent who chooses the smaller-sooner reward on every item therefore
gets an overall k that is not the mean of their magnitude k's (for the
21-item MCQ roughly 0.132 overall vs 0.133 / 0.129 / 0.131 by magnitude).
The 21-item ladder keeps the published item order within each k rank,
which is not strictly ascending in k; it must not be re-sorted.
}
\examples{
score_mcq(mcq27, items = 27)
dat21 <- data.frame(subjectid = 1, questionid = 1:21, response = 1)
score_mcq(dat21, items = 21)
}
