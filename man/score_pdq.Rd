% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pdq.R
\name{score_pdq}
\alias{score_pdq}
\title{Score the 30-item Probability Discounting Questionnaire (PDQ)}
\usage{
score_pdq(
  dat = dat,
  impute_method = "none",
  round = 6,
  random = FALSE,
  trans = "none",
  return_data = FALSE,
  verbose = FALSE
)
}
\arguments{
\item{dat}{Dataframe (longform) with subjectid, questionid (1-30), and
response (0 for the smaller guaranteed reward, 1 for the larger risky
reward)}

\item{impute_method}{One of: "none", "ggm", "GGM", "inn", "INN"}

\item{round}{Numeric specifying number of decimal places
(passed to \code{base::round()})}

\item{random}{Boolean whether to insert a random draw (0 or 1) for NAs.
Default is FALSE}

\item{trans}{Transformation to apply to h values: "none", "log", or "ln".
Default is "none"}

\item{return_data}{Boolean whether to return the original data and new
imputed responses. Default is FALSE.}

\item{verbose}{Boolean whether to print subject and question ids
pertaining to missing data. Default is FALSE.}
}
\value{
If \code{return_data = FALSE} (default), a summary data frame with one
row per subject: pooled \code{overall_h} (see Details), per-block h
(\code{block1_h}, \code{block2_h}, \code{block3_h}), their arithmetic mean (\code{mean_h};
Gray et al.'s recommended composite) and geometric mean (\code{geomean_h}),
pooled and per-block consistency plus their mean
(\code{composite_consistency}), and pooled plus per-block proportions of
risky choices (\verb{block*_proportion} is Gray et al.'s risky choice ratio).
If \code{return_data = TRUE}, a list with \code{results} and \code{data} (the input plus
a \code{newresponse} column reflecting any imputation).
}
\description{
Scores the Madden, Petry, & Johnson (2009) PDQ: three 10-item blocks
(block 1: $20 for sure vs. a chance of $80; block 2: $40 vs. $100;
block 3: $40 vs. $60), each an ascending ladder of h values at
indifference under the hyperbolic odds model \code{V = A / (1 + h * theta)},
\code{theta = (1 - p) / p} (Rachlin, Raineri, & Cross, 1991). Each block is
scored independently by the same consistency-maximization algorithm as
the MCQ scorers; this implementation reproduces the Gray et al. (2016)
scoring syntax lookup tables for every possible response pattern.
}
\details{
Each subject's data must satisfy a strict contract: exactly one
row per canonical question id (30 of them; no duplicates, no unknown
ids, none missing) and responses coded 0, 1, or \code{NA} (numeric, logical,
or character/factor values that coerce to 0/1). Malformed input errors
rather than silently mis-scoring. Contrast with \code{\link[=pdq_to_choice]{pdq_to_choice()}}'s
lenient, ragged contract.

The published scoring (Madden et al., 2009; Gray et al., 2016) has no
overall 30-item ladder; blocks are scored separately, and \code{mean_h} is
Gray et al.'s recommended composite. \code{overall_h} and
\code{overall_consistency} are a beezdiscounting extension: all 30 items are
pooled into a single ascending ladder (exact-rational h order, ties
broken by question id; four item pairs tie exactly) and scored by the
same consistency-maximization algorithm with the repeat-last edge.

INN imputation groups items sharing an h rank (one item per block).
Ladders with remaining \code{NA} responses score \code{NA}; Gray et al. recommend
excluding subjects below 80\% consistency on any block.
}
\examples{
score_pdq(pdq)
}
\references{
Madden, G. J., Petry, N. M., & Johnson, P. S. (2009). Pathological
gamblers discount probabilistic rewards less steeply than matched
controls. \emph{Experimental and Clinical Psychopharmacology, 17}(5),
283-290. \doi{10.1037/a0016806}

Gray, J. C., Amlung, M. T., Palmer, A. A., & MacKillop, J. (2016).
Syntax for calculation of discounting indices from the monetary choice
questionnaire and probability discounting questionnaire. \emph{Journal
of the Experimental Analysis of Behavior, 106}(2), 156-163.
\doi{10.1002/jeab.221}
}
