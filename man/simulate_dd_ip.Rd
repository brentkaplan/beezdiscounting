% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-dd-mixed.R
\name{simulate_dd_ip}
\alias{simulate_dd_ip}
\title{Simulate IP-family mixed-effects discounting data}
\usage{
simulate_dd_ip(
  n_subjects = 60,
  delays = c(7, 30, 180, 365, 730, 1460, 2920),
  log_k_pop = log(0.01),
  sigma_u = 0.6,
  phi = 10,
  sigma_e = 0.1,
  family = c("sltb", "gaussian"),
  equation = c("mazur", "exponential"),
  n_conditions = 1,
  delta_k = NULL,
  seed = NULL
)
}
\arguments{
\item{n_subjects}{Integer; number of subjects.}

\item{delays}{Numeric vector of delays (days) each subject is observed at.}

\item{log_k_pop}{Numeric; population intercept on \verb{log k}.}

\item{sigma_u}{Numeric; SD of the subject random intercept on \verb{log k}.}

\item{phi}{Numeric; SLT-beta precision (\code{family = "sltb"}).}

\item{sigma_e}{Numeric; residual SD on \code{y} (\code{family = "gaussian"}).}

\item{family}{One of \code{"sltb"} (default) or \code{"gaussian"}.}

\item{equation}{One of \code{"mazur"} (default) or \code{"exponential"}.}

\item{n_conditions}{Integer; number of between-subject condition levels. When
\verb{> 1}, a \code{condition} factor is added and subjects are split across levels.}

\item{delta_k}{Numeric vector of length \code{n_conditions}; per-condition shift on
\verb{log k} (the first element is typically \code{0} for the reference level).
Required (non-\code{NULL}) when \code{n_conditions > 1}.}

\item{seed}{Optional integer seed.}
}
\value{
A \link[tibble:tibble]{tibble} with columns \code{id} (factor),
\code{condition} (factor; only when \code{n_conditions > 1}), \code{x} (delay), and
\code{y} (indifference proportion in \verb{[0, 1]}).
}
\description{
Generates long-format indifference-point data from the mixed-effects
discounting model used by \code{\link[=fit_dd_tmb]{fit_dd_tmb()}}. Each subject \code{i} has a random
discount rate \verb{log k_i = log_k_pop + delta_k[condition] + u_i},
\code{u_i ~ N(0, sigma_u^2)}. The mean indifference proportion at delay \code{x} is the
discounting function \code{mu} (Mazur hyperbola or exponential), and observed \code{y}
is drawn from the scale-location-truncated beta (\code{family = "sltb"}) via the
inverse-CDF on the truncated beta, or from a clamped Gaussian
(\code{family = "gaussian"}).
}
\details{
The SLT draw uses the same constants as the C++ template and the verified
reference density: \code{s = 1.0000001}, \code{l = 1e-8}, with
\code{a = mu * phi}, \code{b = (1 - mu) * phi}, and
\code{y = (qbeta(U, a, b) - l) * s} where
\code{U ~ Uniform(pbeta(l, a, b), pbeta(1/s + l, a, b))}.
}
\examples{
# A small SLT-beta mixed-effects discounting dataset (id, x, y)
sim <- simulate_dd_ip(n_subjects = 8, seed = 1)
head(sim)

# Two between-subject conditions with a log-k shift on the second level
sim2 <- simulate_dd_ip(
  n_subjects = 8, n_conditions = 2, delta_k = c(0, log(3)), seed = 2
)

}
