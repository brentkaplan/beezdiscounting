% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate-dd-linear.R
\name{simulate_dd_linear}
\alias{simulate_dd_linear}
\title{Simulate indifference points from the linearized Mazur random-effects model}
\usage{
simulate_dd_linear(
  n_subjects,
  delays,
  mu,
  sigma2,
  g,
  seed = NULL,
  attach_truth = FALSE
)
}
\arguments{
\item{n_subjects}{Integer, scalar (recycled) or one per condition.}

\item{delays}{Positive numeric vector of delays (\code{T = length(delays)}).}

\item{mu}{Numeric vector of population mean ln k per condition; names become condition labels.}

\item{sigma2}{Measurement-error variance on the transformed scale.}

\item{g}{Random-effect variance multiplier: \code{Var(theta) = g * sigma2 / T}.}

\item{seed}{Optional seed; the global RNG state is restored afterwards.}

\item{attach_truth}{If \code{TRUE}, attach \code{attr(, "truth")}.}
}
\value{
Long \code{data.frame(id, condition, x, y)}.
}
\description{
Draws \verb{theta_ic ~ N(mu_c, g sigma2 / T)}, \code{y_ijc ~ N(theta_ic, sigma2)} and
returns \verb{D = 1 / (1 + exp(y) t)} (Hinds et al., 2026, Sec. 4.1), which is
always strictly inside (0, 1).
}
\examples{
s <- simulate_dd_linear(30, c(7, 30, 180, 365), mu = c(A = -6, B = -5), sigma2 = 2, g = 10, seed = 1)
fit_dd_linear(s, factors = "condition")
}
