% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-choice-methods.R
\name{tidy.beezdiscounting_choice}
\alias{tidy.beezdiscounting_choice}
\title{Tidy a beezdiscounting_choice model into a coefficient tibble}
\usage{
\method{tidy}{beezdiscounting_choice}(
  x,
  effects = c("fixed", "ran_pars"),
  report_space = c("natural", "log10", "internal", "log"),
  ...
)
}
\arguments{
\item{x}{A \code{beezdiscounting_choice} object.}

\item{effects}{Character vector: \code{"fixed"} (log-k fixed-effect rows + the
shape rows gamma/beta0), \code{"ran_pars"} (the RE SD), or both (default).}

\item{report_space}{\code{"natural"}, \code{"log10"}, \code{"internal"}, or \code{"log"} -
reporting scale for the fixed-effect \code{estimate}/\code{std.error}. Default is
\code{"natural"}.}

\item{...}{Unused.}
}
\value{
A tibble with exactly 8 columns in this order: \code{term}, \code{estimate},
\code{std.error}, \code{statistic}, \code{p.value}, \code{component}, \code{estimate_scale},
\code{term_display}. Fixed-effect rows carry \code{component == "fixed"}; gamma/beta0
carry \code{component == "shape"}; variance rows carry \code{component == "variance"}.
}
\description{
Returns fixed-effect rows (the log-k coefficients), the shape rows (the
choice-sensitivity \code{gamma} and, when present, the choice-bias \code{beta0}), and
the variance-component row, following the broom coefficient-table contract.
}
\details{
\code{estimate} and \code{std.error} are reported on the \code{report_space} scale for the
fixed-effect (\code{beta_k}) rows and for \code{gamma} (which is transformed
EXPLICITLY since the param-space transformer keys only on k/s/phi). \code{beta0}
is on the identity (logit-intercept) scale and is NEVER transformed across
report spaces. \code{statistic} and \code{p.value} are always computed on the
estimation (internal) scale - Wald statistics are not recomputed after
back-transforming (broom convention).
}
