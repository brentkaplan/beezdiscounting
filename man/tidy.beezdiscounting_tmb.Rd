% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dd-tmb-methods.R
\name{tidy.beezdiscounting_tmb}
\alias{tidy.beezdiscounting_tmb}
\title{Tidy a beezdiscounting_tmb model into a coefficient tibble}
\usage{
\method{tidy}{beezdiscounting_tmb}(
  x,
  effects = c("fixed", "ran_pars"),
  report_space = c("natural", "log10", "internal", "log"),
  ...
)
}
\arguments{
\item{x}{A \code{beezdiscounting_tmb} object.}

\item{effects}{Character vector: \code{"fixed"} (log-k fixed-effect rows),
\code{"ran_pars"} (the RE SD and the auxiliary precision/scale parameter), or
both (default).}

\item{report_space}{\code{"natural"}, \code{"log10"}, \code{"internal"}, or \code{"log"} -
reporting scale for fixed-effect \code{estimate}/\code{std.error} (\code{"internal"} and
\code{"log"} coincide for the log-k \code{beta_k} rows). Default is \code{"natural"}.}

\item{...}{Unused.}
}
\value{
A tibble with exactly 8 columns in this order: \code{term}, \code{estimate},
\code{std.error}, \code{statistic}, \code{p.value}, \code{component}, \code{estimate_scale},
\code{term_display}. Fixed-effect rows carry \code{component == "fixed"}; variance
rows carry \code{component == "variance"}.
}
\description{
Returns fixed-effect rows (the log-k coefficients) and/or variance-component
rows, following the broom coefficient-table contract.
}
\details{
\code{estimate} and \code{std.error} are reported on the \code{report_space} scale for the
fixed-effect (\code{beta_k}) rows. \code{statistic} and \code{p.value} are always computed
on the estimation (log-k) scale - Wald statistics are not recomputed after
back-transforming (broom convention; see the \code{summary()} note for details).
Variance-component rows carry \code{NA} for \code{statistic} and \code{p.value} and are not
affected by \code{report_space}.
}
